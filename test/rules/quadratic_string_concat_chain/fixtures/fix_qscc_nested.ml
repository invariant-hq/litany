(* Four segments right-associate into two matching nodes; containment
   reports only the innermost — one finding per maximal chain,
   anchored at its last three segments. The suite asserts the count and
   the marker reconciliation both. *)

let four = "a" ^ "b" ^ "c" ^ "d" (* FIRE *)
