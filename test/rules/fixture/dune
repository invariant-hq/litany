; The cram fixture: one module per launch rule, each triggering exactly one
; finding, compiled by the outer dune; the cram test walks its artifacts.

(library
 (name fix_rules_cli)
 (modules funcmp length phys warnattr ws))
