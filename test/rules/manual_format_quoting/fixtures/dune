(library
 (name fix_mfq)
 (modules fix_mfq))

; The clippy theorem's compile leg, as a plain dune rule: the committed
; .fixed golden's bytes — here the Unsafe %S rewrite, applied by the suite
; with the unsafe plan — copied to a module name of their own, build as a
; library; the suite depends on the resulting cmt, so `dune runtest`
; exercises the golden's compilation.

(rule
 (copy fix_mfq.fixed.ml fix_mfq_fixed.ml))

(library
 (name fix_mfq_fixed)
 (modules fix_mfq_fixed))
